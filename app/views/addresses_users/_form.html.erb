<%= form_with(model: addresses_user) do |form| %>
  <% if addresses_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(addresses_user.errors.count, "error") %> prohibited this addresses_user from being saved:</h2>

      <ul>
        <% addresses_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :address_id, style: "display: block" %>
    <%= form.text_field :address_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
