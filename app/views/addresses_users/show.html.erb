<p style="color: green"><%= notice %></p>

<%= render @addresses_user %>

<div>
  <%= link_to "Edit this addresses user", edit_addresses_user_path(@addresses_user) %> |
  <%= link_to "Back to addresses users", addresses_users_path %>

  <%= button_to "Destroy this addresses user", @addresses_user, method: :delete %>
</div>
