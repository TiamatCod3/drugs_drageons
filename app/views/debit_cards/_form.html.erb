<%= form_with(model: debit_card) do |form| %>
  <% if debit_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(debit_card.errors.count, "error") %> prohibited this debit_card from being saved:</h2>

      <ul>
        <% debit_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :key, style: "display: block" %>
    <%= form.text_field :key %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
