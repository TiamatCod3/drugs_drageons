<%= form_with(model: pix) do |form| %>
  <% if pix.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pix.errors.count, "error") %> prohibited this pix from being saved:</h2>

      <ul>
        <% pix.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :key, style: "display: block" %>
    <%= form.text_field :key %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
